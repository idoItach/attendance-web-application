export enum UserRoles {
  Manager = 'Manager',
  Employee = 'Employee',
}

export enum ReportStatus {
  InProgress = 'InProgress',
  Pending = 'Pending',
  Approved = 'Approved',
  Rejected = 'Rejected',
}
